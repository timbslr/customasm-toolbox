{
	"cSpell.words": ["addi", "customasm", "ruledef", "Ruledef", "ruledefs", "subruledef"],
	"files.exclude": {
		"out": false
	},
	"search.exclude": {
		"out": true
	},
	"js/ts.tsc.autoDetect": "off" // Turn off tsc task auto detection since we have the necessary tasks as npm scripts
}
